<div class="flex flex-col">
  <div>
    <h2 class="font-semibold text-4xl mb-4">
      <%= @event.name %>
    </h2>
    <span class="text-xl text-gray-500">
      <%= price(@event) %>
    </span>
    <h3 class="text-gray-500 decorat">
      <em><%= day_and_time(@event) %> in <%= @event.location %></em>
    </h3>
    <p class="mt-4">
      <%= @event.description %>
    </p>
  </div>
</div>