<div class="flex flex-col pb-3 border-b-1 border-b-gray-300 mb-4">
  <div>
    <h2 class="font-semibold text-4xl mb-4">
      Editing <%= @event.name %>
    </h2>
    <div class="w-lg">
      <%= render "form" , events: @events %>
    </div>
  </div>
</div>
